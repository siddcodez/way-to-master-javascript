const rollNumber = 124323
let stName = "sidd"
var stEmail = "sidd@gmail.com"// we can't use var because block scope or functional scope. That's why we use let for sure.
let stclass

// rollNumber = 12321 // here we can't change the value of const.

console.table([rollNumber,stEmail,stName,stclass])